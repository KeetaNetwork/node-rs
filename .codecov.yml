# Codecov configuration
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 10%
    patch:
      default:
        target: auto
        threshold: 10%

ignore:
  - "tests/"
  - "examples/"
  - "benches/"

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false
